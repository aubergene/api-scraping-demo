const numPages = 200
// const urls = []

for (let i = 0; i < numPages; i++) {
    const url = `https://se.royalacademy.org.uk/2019/artworks?page=${i}`
    console.log(url)
    // urls.push(url)
}

// console.log(urls)
